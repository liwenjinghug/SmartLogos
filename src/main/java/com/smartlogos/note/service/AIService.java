// service/AIService.java (保留占位，具体实现由独立 AI 服务提供)
package com.smartlogos.note.service;

import org.springframework.stereotype.Service;

@Service
public class AIService {
    // 当前项目不再实现 /api/analyze 和 /api/chat，前端将直接调用独立的 AI 服务。
}
